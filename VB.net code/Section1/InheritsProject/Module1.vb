Module Module1

    Sub Main()
        ''Basic Class Base Class
        'Dim p1 As Person = New Person()
        'p1.FullName = "Werayootk k"
        'p1.ShowData()

        ''Inherits class 
        'Dim p2 As Customer = New Customer()
        'p2.FullName = "Inherrit"
        'p2.ShowData()

        'Dim c1 As Customer = New Customer()
        'c1.FullName = "john"
        'c1.Address = "nonthaburi"
        'c1.ShowDataCust()

        ''Overridies
        'Dim c1 As Customer = New Customer()
        'c1.FullName = "john"
        'c1.Address = "nonthaburi"
        'c1.ShowInfo()

        'Overloading
        'Dim c1 As Customer = New Customer()
        'c1.FullName = "john"
        'c1.Address = "nonthaburi"
        'c1.ShowInfo()
        'c1.ShowData("Werayoot naja")
        'c1.ShowData()



        Console.ReadKey()

    End Sub

End Module
